@model CoffeeLogAppv1.Models.Brew

<h4>Search for a Brew</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ShowSearchResults">
           <!-- Only allowing search by coffee name, might build out other fields later-->
            <div class="form-group">
                <label for="SearchCoffeeName" class="control-label">Coffee Name</label>
                <input name="SearchCoffeeName" class="form-control" />
            </div>
           
            <div class="form-group">
                <input type="submit" value="Search" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

